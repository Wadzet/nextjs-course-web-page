import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="text-center pt-2 text-header-text">
            <h1 className="text-4xl font-bold">Our Pricing</h1>
        </header>
    );
};

export default Header;